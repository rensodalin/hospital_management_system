enum DoctorSpecialization {
  cardiology,
  neurology,
  pediatrics,
  orthopedics,
  dermatology,
  generalMedicine
}

enum AppointmentStatus { scheduled, completed, cancelled, inProgress }

enum PatientStatus { active, discharged, admitted }
